# GeoQuest Frontend Environment Variables
# Copy this file to .env and configure with your values

# WebSocket Server URL
# Socket.IO server URL (uses HTTP/HTTPS, Socket.IO handles WebSocket upgrade)
# For local development: http://localhost:3001
# For Docker development: http://backend:5001
VITE_WEBSOCKET_URL=http://localhost:3001

# Map Provider
# Choose between 'leaflet' (free, no API key required) or 'mapbox' (requires API key)
VITE_MAP_PROVIDER=leaflet

# Mapbox API Key (optional)
# Only required if VITE_MAP_PROVIDER=mapbox
# Get your free API key at: https://www.mapbox.com/
VITE_MAP_API_KEY=
